# Platform M1 验证记录

- **状态**：Verified
- **日期**：2026-09-04
- **分支**：`codex/bootstrap-harness-platform-foundation`
- **验证实现 SHA**：`bafa9da3b567c47ebf689c5d6036a124a149fd1e`
- **范围**：Platform M1 领域、持久化、管理 API、授权、审计、Admin Web 与本地 Thin Host

## 1. 实现检查点

| 提交 | 内容 |
| --- | --- |
| `f3611d5b24b1f45d6a97668cf14898d45707575a` | Harness MVP 领域模型 |
| `180a50e56efd97749c3b2bfa1212d94c2bf95603` | Repository、Migration 与 SQLite 事务 |
| `8c5f00f135acd914b7aa20702562c49f81affd9a` | 管理后端模块与 owner/tenant 隔离 |
| `421d7b727e66225ab4fb70a36ee18efb8442cb1e` 至 `ae8574ec7bdb28b1c43304bf58ec625bf9da497b` | Key Package、Audit、Idempotency、Readiness 与路由授权收口 |
| `ac6a4fc313c3606fe8adbedf5cd445a5aab4bdf6` | 五类 Admin Web 管理页面、Typed API、状态与中英文文案 |
| `0f1437239ec8b0459119911a186a133f119b480c` | SQLite Key Package 并发锁冲突有界重试 |
| `bafa9da3b567c47ebf689c5d6036a124a149fd1e` | Delivery 数字方向枚举的严格解码与合同测试 |

## 2. 当前 SHA 的远端 CI

- push Run `33886744485`：`success`；
- PR Run `33886748998`：`success`；
- 两个 Run 的 documentation、platform-import、protocol、aba-rust、hc-typescript Job 均成功；
- 此 SHA 的 HC Job 仍因没有 `hc/package.json` 而显式跳过，不能记为 HC 已验证。

## 3. 本地静态、单元与构建验证

以下命令在实现 SHA `bafa9da3b567c47ebf689c5d6036a124a149fd1e`、Node `v24.20.0`、pnpm `10.34.5` 环境通过：

```text
go test -count=1 ./...
go vet ./...
go test -race -count=1 ./internal/harness/store ./internal/modules/harness
pnpm lint
pnpm test                 # 4 files / 11 tests
pnpm build
./scripts/check-docs.sh
./scripts/verify-platform-import.sh
```

SQLite 并发回归另执行：

```text
go test -count=50 -run '^TestKeyPackageUniqueScopeIsIdempotentAndTenantBound$' ./internal/harness/store
```

结果通过，未再出现锁冲突导致的偶发失败。

## 4. 官方 Thin Host 验证

使用官方 `mss v1.3.7`，其 `mss`/`mss-mcp` Commit 均为 `77b53d41092741eac62fa6418c0bdbf87413c7cd`；安装资产和 Node `v24.20.0` 下载包均按官方 SHA256 清单核验。

在当前实现 SHA 上实际通过：

```text
mss doctor --strict
mss verify --all
mss upgrade admin v1.3.7 --format json
```

结果：

- Doctor `ready: true`；
- Verify `verification success: true`；
- Upgrade 为 dry-run，Foundation 与 Blueprint 版本不变；
- `conflicts: []`，没有 create/update/delete；
- Harness 后端模块、管理页面、路由和 locale 均列为 downstream preserve；
- Thin Host 固定后端 `admin@v1.3.7`、前端 `admin-web@1.3.7`，无本地 replace 或 vendored Foundation。

## 5. 本地部署与内置浏览器验收

通过官方 `mss setup` 初始化忽略的本地 SQLite 数据库，以 `mss dev --detach` 启动：

```text
backend:   http://127.0.0.1:8080
admin-web: http://127.0.0.1:8001
```

使用内置浏览器完成以下验收：

| 页面/状态 | 实际结果 |
| --- | --- |
| Overview | 空态通过；带测试数据时显示 2 Endpoint、1 Session、1 未 ACK Frame |
| Enrollments | 列表通过；Approve 成功并刷新为 `APPROVED` |
| Endpoints | 列表通过；HC Endpoint Suspend/Resume 均成功 |
| Sessions | Active Session、Runtime、Workspace、Generation 展示正确 |
| Delivery | Session、Frame、ACK 展示正确；方向映射为 `HC_TO_ABA` |
| Error | 查询不存在 Session 显示错误态和 `session was not found` |
| Forbidden | 无 Harness 权限的本地测试用户显示 403 禁止态 |
| Loading/Empty | 共享状态单测覆盖 Loading；五页面空数据态已浏览器验证 |

本地账号、密码、Cookie、测试私钥、Token 和 Ticket 未写入仓库。测试数据仅存在于已忽略的本地数据库。

## 6. 失败、修复与重测

- `ac6a4fc...` 的 push Run `33862678753` 与 PR Run `33862681772` 失败：SQLite 并发测试偶发 `database is locked/deadlocked` 并被归一化为 `HARNESS_CONFLICT`。
- `0f143723...` 增加上下文感知、最大 8 次的 1–128ms 指数重试；50 次定向回归、Go 全量、Race Detector、push Run `33863007760` 和 PR Run `33863012152` 均通过。
- 初次 Delivery 浏览器访问失败：Go JSON 枚举编码为数字 `1`，前端合同只接受字符串。
- `bafa9da3...` 使用闭集数字映射 `1 -> HC_TO_ABA`、`2 -> ABA_TO_HC`，未知值失败关闭；11 个前端测试、浏览器 Delivery 正常态和该 SHA 两个 CI Run 均通过。

## 7. 结论与边界

Platform M1 范围已实现并按上述证据验证，可作为后续 M2/M3 的稳定检查点。此结论不覆盖尚未实现的 DPoP、Credential 签发、Ticket/WSS、跨语言密码学、ABA 数据面、HC、Compose 或完整端到端链路，也不等于整个 MVP 已完成。
